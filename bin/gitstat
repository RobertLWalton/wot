#!/bin/sh
case $1 in -doc*)
    echo "
gitstat
    List all committable files that differ from their
    last committed versions."

    exit 1
    ;;
esac

git status | egrep \
    "^#[ 	]+([a-z ]+:|Chang|.* branch )"
