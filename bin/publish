#!/bin/sh

for x in `find . -name "Publish" -print`
do
    d=`dirname $x`
    for y in `cat $x`
    do
        z=$d/$y
	if [ -x $z ]
	then
	    echo chmod a+rx $z
	    chmod a+rx $z
	else
	    echo chmod a+r $z
	    chmod a+r $z
	fi
    done
done
