#!/bin/sh
if test $# -ne 0
then
    exec git commit -m update "$@"
fi

git status -a | egrep "^#[ 	]+([a-z]+:|Chang)"
echo 'OK (y or n)?'
read
if test "$REPLY" = "y"
then
    git commit -a -m update
else
    echo 'ABORTED!'
fi
